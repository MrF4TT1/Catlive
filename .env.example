# Copia questo file in ".env" e personalizza i valori.

# Porta del server API/produzione
PORT=4000

# Segreto per firmare i token di sessione (JWT).
# IMPORTANTE: in produzione metti una stringa lunga e casuale.
JWT_SECRET=cambia-questo-segreto-con-una-stringa-lunga-e-casuale
